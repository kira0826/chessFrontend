export interface Play {
    id: number;
    origin: string;         
    destination: string;   
    sequenceNumber: number;
    timestamp: string;     
    gain: number;         
    matchId: number;        
    chessCardId: number;    
  }